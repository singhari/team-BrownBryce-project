Site works, regardless of browser used

Result: Pass


The site loads and works as intended regardless of user choice of browser.

Open the website in Firefox, Chrome, and Edge [what we tested on for this testing case]. Run the app in one. Fill in a daily log. Open the app in another browser. Sign in. Check if everything saved. Test navigability in each browser.